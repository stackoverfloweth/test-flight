<h1><%= @student.firstName %> <%= @student.lastName %></h1>

<%= render "form", student: @student %>

<%= button_to 'Delete Student', [@account, @student],
              method: :delete,
              data: { confirm: "Are you sure?" } %>